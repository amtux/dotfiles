# CLAUDE.md

## Rules

- When adding, removing, or changing keymaps, plugins, dependencies, or completion behavior, update `README.md` to reflect the change.
- When adding or removing plugins, update the plugin list in `init.lua` and the Plugins section in `README.md`.
- When adding external tool dependencies, update both `install-deps.sh` and the External Dependencies section in `README.md`.
